class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        n=len(nums)
        seen=set(nums)
        arr=[]
        
        for i in range(1,n+1):                         
            if i not in seen:
                arr.append(i)
        return(sorted(arr))
        
# nums = [8, 1, 2, 2, 3]:

# Sorted: [1, 2, 2, 3, 8]

# Rank: {1:0, 2:1, 3:3, 8:4}

# Result: [4, 0, 1, 1, 3]
